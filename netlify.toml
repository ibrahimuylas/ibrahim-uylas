[build]
  command = "npm run clean --workspace site && npm run build"
  publish = "site/public"

[functions]
  directory = "netlify/functions"

[[headers]]
  for = "/*"

  [headers.values]
    Referrer-Policy = "strict-origin-when-cross-origin"
